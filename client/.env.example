# Base URL of the Express backend's versioned API.
VITE_API_BASE_URL=http://localhost:5000/api/v1

# Base URL used for the Socket.io (real-time chat) connection.
# Usually the same host as the API, without the /api/v1 path.
VITE_SOCKET_URL=http://localhost:5000
